server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/vanderfan/springcloud-learning
          searchPaths: config
      label: master

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false