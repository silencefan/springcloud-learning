spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/vanderfan/springcloud-learning/
          searchPaths: config
          username:
          password:
server:
  port: 8888